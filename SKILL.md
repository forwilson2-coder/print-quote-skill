---
name: 算印刷报价
description: "Use this skill when the user asks to 算印刷报价/算价格/报价 for HP Indigo 对开数码印刷 (骑马订/胶订/锁线胶装/精装). Inputs: 成品尺寸, 总P数, 封面内文纸张克重与单双面, 覆膜, 本数, 装订方式. Output: formatted quote (原始规格→印刷环节→后期→总计)."
metadata:
  builtin_skill_version: "1.0"
  qwenpaw:
    emoji: "💰"
    requires: {}
---

# 算印刷报价（HP Indigo 对开数码印刷）

根据 HP 对开报价表计算骑马订 / 胶订 / 锁线胶装 / 精装册子的价格，输出标准格式报价单。

## 何时使用

用户给出印刷规格要求报价或算价格时。规格包括：成品尺寸、总 P 数、封面/内文纸张克重与单双面、是否覆膜及膜型、本数、装订方式。

**如果数据不充足（缺尺寸、缺总 P 数、缺本数等），直接请用户补充，不要硬算、不要猜。**

## 核心规则（口径，全部经用户确认，勿改）

1. **1P = 1 个成品单面（page），2P = 1 页。** 总 P 数含封面；封面固定 4P（封一~封四），内文 = 总 P − 4。
2. **出血：成品尺寸上下左右各 +3mm 后用于拼版**（如 210×297 → 216×303）。
3. **HP 对开打印面积 = 510 × 740 mm。**
4. **每面 P 数**：按出血后尺寸在 510×740 上排（竖排与旋转 90° 取较大值），如 A4(210×297)=4P/面、140×210=10P/面（2×5 旋转排）。
5. **骑马订、锁线（锁线胶装/硬壳锁线）必须偶数拼版**（页面成对出现，一张纸对折）；奇数 P/面只能是胶订的排法。
6. **张数**：双面印刷，每张对开纸 = 每面P数 × 2 个 P；对开张数 = P数 × 本数 ÷ (每面P数 × 2)，向上取整。
7. **单面印刷：纸照算**——张数与双面相同（背面空白那张纸一样消耗），**仅单价换成单面价**。
8. **价格（元/张·对开，双面/单面）**：
   - 铜版纸：105g 1.6/1.1、128g 1.6/1.2、157g 1.6/1.1、200g 1.9/1.3、250g 2.0/1.4、300g 2.2/1.5
   - 胶版纸：80/100/120g 均 1.6/1.1
   - 白卡纸：230g 2.3/1.6、250g 2.4/1.7、300g 2.6/1.8
   - 自带纸：单面 1.0；**双面 = 单面 × 2 = 2.0**
9. **后期**：
   - 覆膜（亮/哑/相膜）：0.5 元/张·对开，**跟随封面张数**（一次覆一个对开）
   - 骑马钉：0.5 元/本
   - 铜版胶钉：1.5 元/本（**内文是铜版纸时**）；胶版胶钉：1.0 元/本（内文胶版纸时）
   - 精装阶梯（元/本）：1-20本 / 21-49本 / 50-99本 / 100本以上
     - 锁线胶装 10 / 8 / 6 / 5；硬壳打钉精装 20 / 15 / 10 / 8；硬壳蝴蝶精装 20 / 15 / 10 / 9；硬壳锁线精装 20 / 15 / 10 / 9
   - 注意精装第二档是 **21-49本**（20 本整按第一档 10 元）

## 执行

本技能带批处理文件 `scripts/quote.json`（调用 `scripts/quote.py` 一次性算完并输出标准报价单）。

**用 `run_tool_batch` 加载文件执行，不要自己拼 actions。`file_path` 必须用绝对路径**——用你读到本 SKILL.md 时的技能目录绝对路径拼接：

```
run_tool_batch(
  file_path="<此技能目录绝对路径>/scripts/quote.json",
  args={
    "skill_dir": "<此技能目录绝对路径>",
    "w": 210, "h": 297, "p": 24, "copies": 10,
    "cover": "250-copper-double", "inner": "157-copper-double",
    "lamination": "matte", "binding": "saddle"
  }
)
```

**执行前先收集齐规格**：成品尺寸、总 P 数、本数、封面/内文纸张（克重+铜版/胶版/白卡/自带+单/双面）、覆膜（无/亚膜/亮膜）、装订方式。缺任何一项先问用户。

### Batch 参数

* `skill_dir`: 技能目录绝对路径（本 SKILL.md 所在目录），用于定位 `scripts/quote.py`。示例：`/run/csi/mount-root/nas/4079184d856ecc166ed19d4887083405/workspaces/default/skills/算印刷报价`
* `w`, `h`: 成品尺寸 mm（不含出血），如 `210`、`297`
* `p`: 总 P 数（含封面），如 `24`
* `copies`: 本数，如 `10`
* `cover`: 封面纸张规格 `<克重>-<类型>-<面>`，类型 `copper|woodfree|card|self`，面 `double|single`；自带纸写 `self-single`。示例：`250-copper-double`
* `inner`: 内文纸张规格，格式同上。示例：`157-copper-double`
* `lamination`: `none`（无）| `matte`（亚膜）| `glossy`（亮膜）
* `binding`: `saddle`（骑马订）| `glue`（胶订，自动按内文铜/胶版选胶钉价）| `lock`（锁线胶装）| `hard-nail`（硬壳打钉精装）| `hard-butterfly`（硬壳蝴蝶精装）| `hard-lock`（硬壳锁线精装）

### 批处理失败处理

1. 先检查所有参数都传了真实值（尤其 `skill_dir` 是否正确、是否传了空 `args={}`），修正后重试。
2. 仍失败则用 `execute_shell_command` 直接跑 `python3 <skill_dir>/scripts/quote.py ...` 手工执行。
3. 完成后再告诉用户批处理有问题，并建议用 `edit_file` 修脚本。

## 手工参考（批处理内部逻辑，调试用）

1. `parse_spec` 解析纸张规格（克重、类型、单双面）。
2. `per_side_pages`：出血后尺寸排 510×740，竖排与旋转取大；骑马订/锁线只取偶数。
3. `sheets`：对开张数 = P数 × 本数 ÷ (每面P数 × 2) 向上取整（封面 4P、内文 p−4P 分别算）。
4. 单价查 `PAPERS`（自带纸双面=2.0），单面印刷用单面价、张数照旧。
5. 覆膜 = 封面张数 × 0.5；装订按 `bind_cost`（胶钉分铜版 1.5 / 胶版 1.0；精装按本数阶梯）。
6. 输出标准模板（见下）。

## 输出格式（固定模板）

```
📋 原始规格
| 成品尺寸 | <w> × <h> mm（出血后 <bw>×<bh>） |
| 装订方式 | <骑马订/胶订/锁线胶装/...> |
| 总页数 | <p>P（封面 4P + 内文 <p-4>P） |
| 封面 | <克重>g <纸种>，<单/双面>印刷<膜> |
| 内文 | <克重>g <纸种>，<单/双面>印刷 |
| 数量 | <本数> 本 |
| 打印幅面 | HP 对开 510 × 740 mm（<n>P/面） |

📄 印刷环节
| 环节 | 对开张数 | 单/双面 | 单价 | 金额 |
| 封面打印（...） | <n> 张 | ... | ... | ... |
| 内文打印（...） | <n> 张 | ... | ... | ... |
| **印刷小计** | **<n> 张** | | | **<金额> 元** |

🛠 后期
| 项目 | 数量 | 单价 | 金额 |
| 覆膜（亚膜/亮膜） | <封面张数> 张对开 | 0.5 元/张 | ... |
| <装订> | <本数> 本 | ... 元/本 | ... |
| **后期小计** | | | **<金额> 元** |

💰 **总计：<金额> 元**（单本 <金额> 元）
```

## 已验证算例（用户确认正确，可当回归测试）

| 场景 | 参数 | 期望结果 |
|---|---|---|
| 210×297 骑马订 24P×10，封面 250 铜双面+亚膜，内文 157 铜双面 | `saddle` | 57.5 元（单本 5.75） |
| 210×285 骑马订 20P×30，封面 200 铜单面，内文 105 铜双面 | `saddle` | 130.5 元（单本 4.35） |
| 140×210 骑马订 16P×100，封面 250 铜双面+亮膜，内文 105 铜双面 | `saddle` | 196 元（单本 1.96） |
| 210×297 胶订 64P×50，封面 250 铜双面+亚膜，内文 157 铜双面 | `glue` | 737.5 元（单本 14.75） |
| 210×285 锁线胶装 72P×40，封面 250 铜单面+亚膜，内文 105 铜双面 | `lock` | 902 元（单本 22.55） |

## 注意事项

* 价格表有更新时，同步改 `scripts/quote.py` 里的 `PAPERS` / `LAM_PRICE` / `BIND_NAME` / 阶梯价格，并更新本 SKILL.md。
* 报价表与口径的权威来源是工作区 `MEMORY.md` 的「印刷报价」章节，两者冲突时以 MEMORY.md 和用户口头确认为准。
* 单本价保留合适精度（整数或小数，去掉多余 0）。
