# 算印刷报价（HP Indigo 对开数码印刷）

根据 HP 对开报价表（xlsx）计算骑马订 / 胶订 / 锁线胶装 / 精装册子的价格，输出标准格式报价单。

## 核心规则（计价口径）

1. **1P = 1 个成品单面，2P = 1 页。** 总 P 数含封面；封面固定 4P，内文 = 总 P − 4。
2. **出血：成品尺寸上下左右各 +3mm 后用于拼版**（如 210×297 → 216×303）。
3. **HP 对开打印面积 = 510 × 740 mm。**
4. **每面 P 数**：按出血后尺寸在 510×740 上排（竖排与旋转 90° 取较大值），A4=4P/面、140×210=10P/面。
5. **骑马订、锁线必须偶数拼版**；奇数 P/面只能是胶订的排法。
6. **张数**：双面印刷每张对开纸 = 每面 P 数 × 2；对开张数 = P数 × 本数 ÷ (每面P数 × 2)，向上取整。
7. **单面印刷：纸照算**（张数与双面相同），仅单价换成单面价。
8. **价格全部从 xlsx 报价表读取**，不在脚本里硬编码。
9. **后期**：覆膜按封面张数计（元/张·对开）；骑马钉 / 铜版胶钉 / 胶版胶钉按本；精装按 1-20 / 21-49 / 50-99 / 100本以上 四档阶梯。

## 价格来源

价格从 xlsx 表格读取，**改表格即改价，不碰脚本**。

- 脚本：`scripts/quote_xlsx.py`（依赖 `openpyxl`，其余标准库）
- 空模板：`scripts/prices_template.xlsx`——表头与克重行已建好、价格留空，复制后填价即可使用

### 表格结构（Sheet1）

| 位置 | 内容 |
|---|---|
| A~C 列 | 铜版：克重（如 `250g`）+ 双面/单面单价（元/张·对开） |
| D~F 列 | 胶版纸：克重 + 双面/单面单价 |
| G~I 列 | 白卡纸：克重 + 双面/单面单价 |
| J 列 | 自带纸单面价 |
| K 列 | 自带纸双面价（可选；不填则按单面 ×2） |
| D8 / E8 / F8 | 骑马钉 / 铜版胶钉 / 胶版胶钉（元/本） |
| E10 | 覆膜单价（元/张·对开，亮/哑/相膜同价） |
| 14~17 行 | 精装阶梯（锁线胶装 / 硬壳打钉 / 硬壳蝴蝶 / 硬壳锁线；列 C/E/F/G = 1-20 / 21-49 / 50-99 / 100本以上） |

## 用法

```bash
python3 scripts/quote_xlsx.py <报价表.xlsx> w h p copies cover inner lamination binding
```

参数：
- `<报价表.xlsx>`：价格表路径（必填，第一个参数）
- `w` / `h`：成品尺寸 mm（不含出血），如 `210` / `297`
- `p`：总 P 数（含封面），如 `24`
- `copies`：本数，如 `10`
- `cover` / `inner`：纸张规格 `<克重>-<类型>-<面>`，类型 `copper|woodfree|card|self`（自带纸写 `self-single`），面 `double|single`；示例 `250-copper-double`
- `lamination`：`none`（无）| `matte`（亚膜）| `glossy`（亮膜）
- `binding`：`saddle`（骑马订）| `glue`（胶订，按内文铜/胶版自动选胶钉价）| `lock`（锁线胶装）| `hard-nail`（硬壳打钉精装）| `hard-butterfly`（硬壳蝴蝶精装）| `hard-lock`（硬壳锁线精装）

示例（A4 骑马订 24P × 10 本）：

```bash
python3 scripts/quote_xlsx.py scripts/prices_template.xlsx 210 297 24 10 250-copper-double 157-copper-double matte saddle
```

## 输出格式

```
📋 原始规格
| 成品尺寸 | <w> × <h> mm（出血后 <bw>×<bh>） |
| 装订方式 | <装订> |
| 总页数 | <p>P（封面 4P + 内文 <p-4>P） |
| 封面 | <克重>g <纸种>，<单/双面>印刷<膜> |
| 内文 | <克重>g <纸种>，<单/双面>印刷 |
| 数量 | <本数> 本 |
| 打印幅面 | HP 对开 510 × 740 mm（<n>P/面） |

📄 印刷环节
| 环节 | 对开张数 | 单/双面 | 单价 | 金额 |
| **印刷小计** | **<n> 张** | | | **<金额> 元** |

🛠 后期
| 项目 | 数量 | 单价 | 金额 |
| **后期小计** | | | **<金额> 元** |

💰 **总计：<金额> 元**（单本 <金额> 元）
```

## 依赖与注意

- 依赖 `openpyxl`：`pip install openpyxl`
- 用户可能自行改动表格结构，解析失败时先核对结构（克重行/后期行/精装行位置）
- 单本价输出保留合适精度（去掉多余 0）